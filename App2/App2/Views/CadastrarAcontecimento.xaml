<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:App2.ViewModel"
             x:Class="App2.Views.CadastrarAcontecimento"
             Title="Cadastrar">
    <ContentPage.BindingContext>
        <vm:CadastrarAcontecimentoViewModel></vm:CadastrarAcontecimentoViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Picker x:Name="Acontecimento"  Title="Escolha" ItemsSource="{Binding Acontecimentos}" SelectedItem="{Binding CadAcontecimentoModel.Partida__c}" ItemDisplayBinding="{Binding Name}" ></Picker>
            <Picker x:Name="Jogadores"  Title="Escolha" ItemsSource="{Binding Jogadores}" SelectedItem="{Binding CadAcontecimentoModel.Jogador__c}" ItemDisplayBinding="{Binding Apelido__c}" ></Picker>
            <Entry Text="{Binding CadAcontecimentoModel.Tempo_do_acontecimento__c}"></Entry>
            <Button Command="{Binding CadastrarClickedCommand}" Text="Cadastrar"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>